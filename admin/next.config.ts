import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Admin körs på port 3001 (se package.json "dev")
  // Inga NEXT_PUBLIC_USER_* variabler ska finnas här
};

export default nextConfig;
